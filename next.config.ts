import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["picsum.photos", "i.pravatar.cc", "dream-coaching.vercel.app", "images.unsplash.com", "imgs.search.brave.com"],
  },
};

export default nextConfig;
